server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA — 없는 경로는 index.html로 폴백해 클라이언트 라우팅(react-router)에 맡긴다.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
